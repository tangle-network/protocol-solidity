yarn setup:vbridge2 \
  && yarn setup:vbridge8
  # && yarn setup:bridge2
  # && yarn setup:bridge3
  # && yarn setup:bridge4
  # && yarn setup:bridge5
  # && yarn setup:bridge6
  # && yarn setup:semaphore-bridge2