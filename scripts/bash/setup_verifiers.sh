# yarn setup:vanchor2 \
#   && yarn setup:vanchor8
  # && 
  yarn setup:bridge2 \
  && yarn setup:bridge3 \
  && yarn setup:bridge4 \
  && yarn setup:bridge5 \
  && yarn setup:bridge6
  # && yarn setup:semaphore-bridge2